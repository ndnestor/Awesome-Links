
export class statusCodes {

    // Success responses
    static OK = 200;
    static CREATED = 201;

    // Client errors
    static BAD_REQUEST = 400;
    static NOT_FOUND = 404;

    // Server errors
    static INTERNAL_SERVER_ERROR = 500;
}
